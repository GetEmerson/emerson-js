//     Emerson Util
//
//     ...

(function(ns){

  // Emerson Extension
  // --------------------------------------------------------------------------

  // ### Emerson.util module
  //   * `ns` is a reference to the namespace.
  //   * `init` is a hook for initializing the module.
  var util = ns.util = {
    ns   : ns,
    init : function init() {}
  };
})(Emerson);
